rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // --- OPEN ACCESS FOR PROTOTYPING ---
    // This allows anyone to read and write all data.
    // Use this to unblock development while troubleshooting auth-related permission loops.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}